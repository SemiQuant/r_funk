% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_plots.R
\name{volcano_plot}
\alias{volcano_plot}
\title{Create Volcano Plot}
\usage{
volcano_plot(
  data,
  id_col = "ID",
  pvalue_col = "pvalue",
  padj_col = "padj",
  logfc_col = "log2FoldChange",
  padj_threshold = 0.05,
  effect_size_threshold = 2,
  num_annotate = 10,
  title = NULL,
  make_plot = TRUE
)
}
\arguments{
\item{data}{A data frame containing differential expression results}

\item{id_col}{Column name for gene/feature IDs (default: "ID")}

\item{pvalue_col}{Column name for p-values (default: "pvalue")}

\item{padj_col}{Column name for adjusted p-values (default: "padj")}

\item{logfc_col}{Column name for log2 fold change (default: "log2FoldChange")}

\item{padj_threshold}{Significance threshold for adjusted p-value (default: 0.05)}

\item{effect_size_threshold}{Log2 fold change threshold for significance (default: 2)}

\item{num_annotate}{Number of top genes to annotate (default: 10)}

\item{title}{Plot title (default: NULL)}

\item{make_plot}{If TRUE, return ggplot object; if FALSE, return data frame (default: TRUE)}
}
\value{
A ggplot object or data frame
}
\description{
Creates a volcano plot using ggplot2 with customizable columns, annotations, and threshold lines
}
\examples{
# Create toy data
toy_data <- create_toy_volcano_data()

# Basic volcano plot
volcano_plot(toy_data)

# Custom column names and thresholds
volcano_plot(toy_data, padj_threshold = 0.01, effect_size_threshold = 1.5)

# Annotate more genes
volcano_plot(toy_data, num_annotate = 20)

# Get data instead of plot
volcano_data <- volcano_plot(toy_data, make_plot = FALSE)
}
